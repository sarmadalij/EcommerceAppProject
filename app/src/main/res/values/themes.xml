<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EcommerceAppProject" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.EcommerceAppProject" parent="Base.Theme.EcommerceAppProject">
            <!--    we can change color of status bar -->
            <item name="android:statusBarColor">
                @color/statusbar
            </item>
    </style>

    <style name="App.Custom.Indicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">#00000000</item>
        <item name="android:width">50dp</item>
        <item name="android:height">50dp</item>
    </style>
</resources>